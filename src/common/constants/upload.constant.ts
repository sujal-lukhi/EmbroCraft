export const UPLOAD_CONSTANTS = {
  MAX_FILE_SIZE: 10 * 1024 * 1024, // Increased to 10MB just in case embroidery files are large
  ALLOWED_FILE_TYPES: /\.(jpg|jpeg|png|gif|webp|dst|pes|jef|exp|hus|vp3|vip|xxx)$/i,
};
